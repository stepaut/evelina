namespace Db
{
    public enum EItemLevel
    {
        Portfolio = 0,
        Asset = 1,
        Transaction = 2,
    }

    public enum ETransaction
    {
        Buy = 0,
        Sell = 1,
    }
}
